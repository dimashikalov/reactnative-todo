import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  todoContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginVertical: 10,
  },
  todoText: {
    marginLeft: 15,

    fontSize: 20,
  },
});
