import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  textField: {
    borderColor: 'black',
    borderWidth: 1,
    borderRadius: 15,
    fontSize: 20,
    paddingHorizontal: 10,
  },
});
