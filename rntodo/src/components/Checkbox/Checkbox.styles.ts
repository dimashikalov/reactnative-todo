import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  box: {
    height: 20,
    width: 20,
    borderRadius: 15,
    borderWidth: 2,
  },
  checked: {
    backgroundColor: 'black',
  },
});
